# Set after applying the primary env and running the bootstrap to create state buckets.
primary_state_bucket = "REPLACE_WITH_STATE_BUCKET"

tags = {
  Owner      = "platform-team"
  CostCenter = "migration-project"
}
